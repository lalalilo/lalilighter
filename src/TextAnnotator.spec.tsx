import React from 'react';
import { render } from '@testing-library/react';
import TextAnnotator from './TextAnnotator';

describe('TextAnnotator', () => {
  it('renders', () => {
    render(<TextAnnotator />);
  });
});
